
CREATE SCHEMA IF NOT EXISTS LEAGUEPONG;

USE LEAGUEPONG;

CREATE TABLE IF NOT EXISTS USUARIOS(
    ID_USUARIO BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    NOMBRE_USUARIO VARCHAR(255),
    PASSWORD VARCHAR(255),
    MAIL VARCHAR(255),
    LOCALIDAD VARCHAR(50),
    NIVEL INT(50),
    CONSTRAINT NIVEL CHECK (NIVEL BETWEEN 1 AND 4)
);

INSERT INTO USUARIOS (NOMBRE_USUARIO, PASSWORD, MAIL, LOCALIDAD, NIVEL) VALUES 
('FRANCOLACHUPA', 'FRANCOLACHUPA',  'FRANCOLACHUPA@GMAIL.COM', 'BARCELONA', 3),
('VICTOR', '1234', 'MAIL@MAIL.COM', 'BARCELONA', 2.5);
SELECT * FROM USUARIOS;